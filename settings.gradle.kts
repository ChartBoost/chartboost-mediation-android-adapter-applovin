pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

rootProject.name = "AppLovinAdapter"
include(":AppLovinAdapter")
include(":android-helium-sdk")
include(":Helium")
